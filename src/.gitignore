# 📦 Папка с установленными зависимостями npm/pnpm/yarn.
# Не хранится в репозитории, так как её можно восстановить по package.json и lock-файлу.
node_modules

# 🔑 Файл с переменными окружения (ключи API, пароли, конфиги).
# Никогда не коммитится в git по соображениям безопасности.
.env

# 📊 Отчёты о покрытии кода тестами (генерируются инструментами вроде Vitest/Jest).
# Не нужны в репозитории, так как создаются заново при каждом прогоне тестов.
coverage

# 🏗 Папка с собранными файлами (бандлы, оптимизированные ресурсы).
# Генерируется сборщиком (Vite/Webpack/Rollup) и не хранится в git.
dist

# ⚙️ Папка с артефактами сборки (Node.js, фронтенд или серверные билды).
# Обычно используется для production-версии приложения.
build
