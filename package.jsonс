{
  // 📛 Имя проекта. Используется npm, в логах CI/CD, при публикации пакета.
  // Важно: в публичных пакетах должно быть уникальным.
  "name": "devops-node-starter",

  // 🔢 Версия проекта. Участвует в семантическом версионировании (semver).
  // CI/CD пайплайны могут использовать её для автоматической сборки и релизов.
  "version": "1.0.0",

  // 📦 Тип модулей. "module" включает поддержку ES-модулей (import/export).
  // В Docker-образах и CI окружениях это исключает смешение с CommonJS.
  "type": "module",

  // ⚙️ NPM-скрипты для автоматизации задач разработки и деплоя.
  "scripts": {
    // ▶️ Запуск приложения в продакшн-режиме.
    // В Docker CMD или на сервере обычно вызывается именно этот скрипт.
    "start": "node src/index.js",

    // 🛠 Запуск в режиме разработки с авто-перезапуском при изменениях.
    // Удобно для локальной отладки, но в CI/CD не используется.
    "dev": "nodemon src/index.js",

    // 🧪 Запуск тестов с помощью Vitest.
    // В CI пайплайнах этот шаг часто обязателен перед деплоем.
    "test": "vitest",

    // 🔍 Проверка кода линтером ESLint.
    // В CI можно настроить как "quality gate" для pull request'ов.
    // Анализирует код без запуска — читает твои .js/.ts файлы и ищет проблемы.
    // Сравнивает с набором правил — эти правила задаются в конфиге .eslintrc.* (например, «всегда ставить точку с запятой», «не использовать var»).
    // Находит баги и “code smells” — например, неиспользуемые переменные, опечатки в импортах, лишние console.log.
    // Следит за единым стилем — чтобы весь проект выглядел одинаково, независимо от того, кто писал код.
    // Это значит:

    // Запусти ESLint для всех файлов в текущей папке (.).

    // Если найдёт ошибки — выведет их в консоль.

    // В CI/CD пайплайне такой шаг часто ставят перед тестами и сборкой, чтобы не пропускать “грязный” код в репозиторий.
    "lint": "eslint .",

    // 🧹 Линт с автоматическим исправлением ошибок форматирования.
    // Полезно перед коммитом или в pre-commit hook (husky, lint-staged).
    "lint:fix": "eslint . --fix"
  },

  // 📦 Продакшн-зависимости — нужны для работы приложения в runtime.
  "dependencies": {
    // Express — минималистичный веб-фреймворк для Node.js.
    // Версия ^4.18.2: стабильная, совместима с большинством middleware.
    // В Docker-образах важно фиксировать версию для воспроизводимости.
    "express": "^4.18.2"
  },

  // 🛠 Dev-зависимости — нужны только при разработке и тестировании.
  "devDependencies": {
    // ESLint — анализатор кода для соблюдения стиля и поиска ошибок.
    // В CI можно запускать как отдельный шаг перед тестами.
    "eslint": "^9.31.0",

    // globals — список глобальных переменных для ESLint.
    // Удобно, чтобы линтер не ругался на process, window и т.п.
    // 📌 Что такое globals в контексте ESLint
    // ESLint при проверке кода анализирует каждую переменную и, если видит, что она используется, но нигде не объявлена, выдаёт ошибку:
    // Но в JavaScript есть глобальные переменные, которые предоставляет среда выполнения:
    // В Node.js: process, __dirname, Buffer
     // В браузере: window, document, localStorage
     // Чтобы ESLint понимал, что эти переменные разрешены и не являются ошибкой, их нужно явно указать 
     // в конфигурации ESLint как глобальные.
     //  Пакет globals
    // Пакет globals — это готовый справочник всех стандартных глобальных переменных для разных окружений 
    // (Node, браузер, ES2021, WebWorker и т.д.).
     // Вместо того чтобы вручную перечислять десятки переменных в .eslintrc, можно просто подключить этот пакет
    "globals": "^16.3.0",

    // nodemon — перезапускает приложение при изменении файлов.
    // В Docker для разработки можно монтировать volume и использовать nodemon.
    // Что делает:
     // Следит за изменениями в файлах проекта и автоматически перезапускает Node.js‑приложение.
    // Работает как обёртка над node, но с функцией “watcher”.
    "nodemon": "^3.1.10",

    // supertest — тестирование HTTP API без поднятия реального сервера.
    // В CI позволяет быстро проверять эндпоинты.
//     Что делает:

// Позволяет тестировать HTTP API без необходимости поднимать сервер на реальном порту.

// Работает напрямую с экземпляром Express/Koa/Fastify‑приложения, отправляя запросы и проверяя ответы.
    "supertest": "^6.3.3",

    // Vitest — быстрый тестовый раннер, совместимый с Jest API.
    // Хорошо интегрируется с TypeScript и ESM.
//     Что делает:

// Современный тест‑раннер для JavaScript/TypeScript, совместимый с API Jest (describe, it, expect).

// Очень быстрый за счёт использования Vite под капотом.

// Поддерживает ESM, TypeScript, JSX, Vue, React и т.д. без сложной конфигурации.
    "vitest": "^3.2.0"
  }
}
